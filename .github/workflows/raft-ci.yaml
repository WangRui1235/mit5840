name: Raft-CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-raft:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        lab: [ "3A", "3B", "3C", "3D"] 
    
    steps:
    - uses: actions/checkout@v4
    - name: Set up Go 1.22
      uses: actions/setup-go@v5
      with:
        go-version: '1.22'
    - name: Install dependencies
      run: /
        cd src
        go get .
    - name: Run Tests
      run: |
        cd src/raft1
        go test -v -race -run ${{ matrix.lab }}
    - name: Upload Logs on Failure
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: test-log-${{ matrix.lab }}
        path: src/raft1/logs/*.log 